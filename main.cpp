// This is the entrypoint to our program, and the argc represents the argument
// count, while argv represents the different values of the arguments.
#include "src/core.hpp"

using namespace taskmat;

int main(int argc, char **argv) { return run(argc, argv); }
